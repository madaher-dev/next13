let defaultTheme = { primary: "#BFFF38" };

let defaultIcon = (isMedium) =>
  isMedium
    ? "/images/icons/Lokalee-icon-m.svg"
    : "/images/icons/Lokalee-icon-d.svg";

export { defaultTheme, defaultIcon };
